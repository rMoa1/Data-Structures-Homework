	Creating a list
struct Node {
    int data;
    struct Node *next;
};

struct Node *head = NULL; 

int main() {
    struct Node *list_start = NULL; 
    
    return 0;
}

	Traversal
void traverseList() {
    struct Node *current = head;
    printf("List: ");
    if (current == NULL) {
        printf("Empty\n");
        return;
    }
    while (current != NULL) {
        printf("%d -> ", current->data);
        current = current->next;
    }
    printf("NULL\n");
}

	Complexity - it shows how complex the code is. Mostly in commets //...//

	Inserting elements(Front, End)
void insertFront(int data) {
    struct Node *newNode = createNode(data);
    newNode->next = head; 
    head = newNode;       
}


void insertEnd(int data) {
    struct Node *newNode = createNode(data);
    
    if (head == NULL) {
        head = newNode;
        return;
    }
    
    struct Node *current = head;
    while (current->next != NULL) {
        current = current->next;
    }
    
    current->next = newNode;
}

	Deletion operations
void deleteNode(int key) {
    struct Node *temp = head, *prev = NULL;


    if (temp != NULL && temp->data == key) {
        head = temp->next; 
        free(temp);        
        return;
    }
